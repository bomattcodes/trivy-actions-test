name: Trivy Scan (Changes Only)
on:
  push:
    branches:
      - master
  pull_request:


jobs:
  # Job 1 Compute Changes
  dif:
    name: dif
    runs-on: ubuntu-20.04
    outputs:
      all: ${{ steps.changes.outputs.all}}
      ts: ${{ steps.changes.outputs.ts }}

    steps:
      # Checkout Code
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 2 # retrieve the preceding commit.

      # DIF
      - name: Get changed files
        id: changes
        # Set outputs using the command.
        run: |
          echo "::set-output name=all::$(git diff --name-only --diff-filter=ACMRT ${{ github.event.pull_request.base.sha }} ${{ github.sha }} | xargs)"
          echo "::set-output name=ts::$(git diff --name-only --diff-filter=ACMRT ${{ github.event.pull_request.base.sha }} ${{ github.sha }} | grep .ts$ | xargs)"

  scan:
    name: scan
    runs-on: ubuntu-latest
    # require the first job to have ran
    needs: dif
    # only run there are changed files
    if: ${{needs.changedfiles.outputs.ts}}
    steps:
      - name: echo changed files
        run: echo ${{needs.changedfiles.outputs.ts}}

    # Job 2 Run Changes Through Trivy

      # Run Trivy Scan in non-failure mode
      #- name: Run Trivy vulnerability scanner in repo mode
      #  uses: aquasecurity/trivy-action@master
      #  with:
      #    scan-type: 'fs'
      #    ignore-unfixed: true
      #    format: 'sarif'
      #    output: 'trivy-results.sarif'

      # Upload Findings to GitHub
      #- name: Upload Trivy scan results to GitHub Security tab
      #  uses: github/codeql-action/upload-sarif@v2
      #  if: always()
      #  with:
      #    sarif_file: 'trivy-results.sarif'